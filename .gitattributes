# 行尾统一为 LF。本机 core.autocrlf=true 会让 Windows 检出成 CRLF，
# 显式声明可以避免跨平台 diff 噪音，也保证 CI（Linux）与本地一致。
* text=auto eol=lf

*.js    text eol=lf
*.mjs   text eol=lf
*.json  text eol=lf
*.yml   text eol=lf
*.yaml  text eol=lf
*.md    text eol=lf

# 二进制，不做行尾转换
*.tgz   binary
*.png   binary
*.jpg   binary
*.ico   binary
